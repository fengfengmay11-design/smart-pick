# 复制本文件为 .env 后填入你的真实 Key（.env 已在 .gitignore，不会上传）
# ── DeepSeek（推荐：国内直连，新用户送额度）──
LLM_API_KEY=sk-你的key
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# ── 换成通义千问 ──
# LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# LLM_MODEL=qwen-plus

# ── 换成 Kimi ──
# LLM_BASE_URL=https://api.moonshot.cn/v1
# LLM_MODEL=moonshot-v1-8k

# ── 换成 OpenAI ──
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4o-mini

# ── 京东联盟（价格刷新 / 商品查询）──
# 完整申请步骤见 docs/JD_UNION_SETUP.md
#   1. https://union.jd.com/ 京东账号登录 → 实名认证
#   2. 我的推广 → 推广管理 → 网站管理 → 创建网站 → 点「查看」拿 key
#   3. 权限申请 https://union.jd.com/openplatform/groupApply → 选「导购类」
#   4. 填完跑 python server/verify_setup.py 自检
#
# 注意：appkey 是 32 位十六进制串。禁止提交到 GitHub（京东会封禁）。
JD_APP_KEY=your_app_key_here
JD_APP_SECRET=your_app_secret_here
JD_SITE_ID=your_site_id_here
